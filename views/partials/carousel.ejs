<div id="carouselExampleCaptions" class="carousel slide" data-bs-ride="false">
    <div class="carousel-indicators">
      <% sliders.forEach((slider,index) => {%>
      <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="<%= index %> " class="<%=slider.class %>" aria-current="true" aria-label="Slide 1"></button>
      <% }); %> 
    </div>
    <div class="carousel-inner">

      <% sliders.forEach(slider => {%>
        <div class="carousel-item <%= slider.class %> ">
          <img src="<%= slider.imgUrl  %> " class="d-block w-100 slider-image" alt="">
          <div class="carousel-caption d-none d-md-block">
            <h5><%= slider.title  %></h5>
            <p><%= slider.subTitle  %></p>
          </div>
        </div>
      <% }); %> 
    
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Next</span>
    </button>
  </div>